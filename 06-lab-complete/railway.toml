[build]
builder = "NIXPACKS"

[deploy]
# Dùng sh -c để shell expansion $PORT hoạt động đúng
startCommand = "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
